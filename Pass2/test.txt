nat_ind:
    (P : nat → Prop) →
    (* : P 0) →
    (* : (n : nat) -> (* : P n) -> (* : P (S n))) →
    (n : nat) →
    (* : P n).

Now consider

    [$A:%] → forall ($x:A), R x.

This translates to

    [$A:%] → ($x:A) → R x.